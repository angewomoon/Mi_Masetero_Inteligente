<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio externo de la aplicación -->
    <external-path
        name="external_files"
        path="." />
    
    <!-- Directorio de archivos externos privados de la aplicación -->
    <external-files-path
        name="external_app_files"
        path="." />
    
    <!-- Directorio de caché externo -->
    <external-cache-path
        name="external_cache"
        path="." />
    
    <!-- Directorio de archivos internos -->
    <files-path
        name="internal_files"
        path="." />
    
    <!-- Directorio de caché interno -->
    <cache-path
        name="internal_cache"
        path="." />
</paths>
